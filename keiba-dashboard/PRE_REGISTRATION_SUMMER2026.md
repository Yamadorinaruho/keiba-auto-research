# 事前登録 — 夏3戦略v2 2026年フォワードの撤退基準

**登録日: 2026-07-02**
**このドキュメントは登録日以降のフォワード実測に対する判定基準を固定する。結果を見た後の基準変更は無効。**

---

## 対象

本番稼働中の夏3戦略 v2(血統フィルタ、仕様は `live/strategy_spec.py` が単一情報源):

| 戦略 | 稼働窓 | 条件(要約) |
|---|---|---|
| 芝 | 6/16〜8/31 | 全会場×芝未勝利×3歳牝×単勝15-80倍×3走目以上×父{ディープ/サンデー他/カナロア} |
| ダート | 6/16〜8/31 | 全会場×ダ≤1400×未勝利〜OP×3歳牝×単勝10-80倍×3走目以上×父米国系 |
| 新馬 | 6/1〜8/31 | 全会場×芝2歳新馬×父{エピファネイア, エフフォーリア} 全頭・オッズ不問 |

## 判定データ

- **稼働窓内のフォワード実測のみ**を使う(窓外ピックは参考値であり判定に含めない)
- 集計は `python3 -m live.strat_eval --season`(確定オッズ・1点フラット換算)
- 判定は**的中数ベース**(オッズ非依存で検出力計算が明確なため)

## 登録時点のスナップショット(透明性のため記録)

登録日時点で6月分の実測が既に存在する: 芝ダ窓内 n=46・的中1(ROI41%)、新馬 n=18・的中5(ROI119%)。
基準はこの実測を含む累計に適用するが、基準の数値自体は今後変更しない。

## 撤退基準 (`strategy_spec.STOP_RULES` に実装済み・`--summary`で自動判定)

各基準は「窓内累計nが下限に到達した時点で、的中数が上限以下なら発動」。

| # | 対象 | n下限 | 的中上限 | アクション | 根拠 |
|---|---|---|---|---|---|
| 1 | 芝+ダ合算 | 100 | 0 | **投票停止** | 的中率6%仮定で P(0/100)≈0.2% |
| 2 | 芝+ダ合算 | 200 | 4 | **ユニット半減** | 的中率6%仮定で P(≤4/200)≈0.8% |
| 3 | 新馬 | 60 | 5 | **投票停止** | 的中率19.7%仮定で P(≤5/60)≈2% |

補足:
- 基準1は6/28の的中1で既にクリア済み(発動しないことが確定)
- 破産防御は別レイヤーで常時有効(残高0.5%日次ユニット, `live/bankroll.py`)
- 基準発動後の再開は、2027年シーズン前の再検証(2026実測込みOOS)を通過した場合のみ

## 仕様凍結

- 2026シーズン中(〜8/31)、v2の条件(帯・血統・キャリア・窓)は**変更しない**
- 条件変更のアイデアは2027年仕様候補としてdecision/materialに記録するだけに留める
- 例外は「実装バグの修正」のみ(仕様と実装の乖離を仕様側に合わせる変更は禁止)

## 履行ログ

- [x] 2026-07-02 基準登録・`strategy_spec.STOP_RULES` 実装・`strat_eval --summary` 判定表示
- [ ] シーズン終了時(8/31後)に最終判定を本ファイル末尾へ追記
