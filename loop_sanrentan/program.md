# keiba_loop_trifecta

競馬三連単の実運用耐久性を最大化する自律実験ループ。

## Files

- **`prepare.py`** — 固定。データ読み込み・シャッフル・評価・可視化。変更しない。
- **`train.py`** — 唯一編集するファイル。特徴量・モデル・賭け戦略のすべてがここにある。
- **`Data_2020_fixed.csv`** — 元データ。CSVは着順順に並んでいるがprepare.pyでシャッフル済み。

## Goal

**主指標: Sortino ratio（月次回収率ベース）を最大化する。**

Sharpeではなく Sortino を使う理由: Sharpe は上振れ（大勝ち月）もリスクとしてカウントする。
競馬のように「たまに大きく当たる」構造では、下振れだけを測る Sortino の方が実態に合う。

副指標（重要度順）:
1. **MDD（最大ドローダウン）** — ピーク利益からの最大下落率。低いほど良い。目標: 30%以下
2. **年間利益額（円）** — ROIが高くてもレース数が少なければ意味がない。回転率を考慮した絶対額
3. **最大連続赤字月** — 実運用の心理的耐久性。目標: 3以下
4. **破産確率** — ケリー基準ベース。100単位開始で実質0であること
5. **ROI（回収率%）** — 100%超がプラス収支

制約:
- ROI >= 100%（赤字は絶対NG）
- 破産確率(100単位) < 0.01（これを超えるなら実運用不可）
- 実運用前提 — バックテスト過学習に注意。テスト期間は2024-01〜2025-10。

## Evaluation metrics（train.pyが出力する）

```
roi:              156.32%      # 回収率
hit_rate:         4.05%        # 的中率
sharpe_ratio:     1.781        # シャープレシオ（参考値）
sortino:          4.089        # ソルティノレシオ（主指標）
mdd_pct:          11.4%        # 最大ドローダウン（対ピーク%）
max_losing_streak: 3           # 最大連続赤字月
ruin_prob_100:    0.000000     # 破産確率(100単位開始)
annual_profit:    +129,801円   # 年間想定利益
annual_bets:      606.5        # 年間レース数
```

## What you CAN edit

`train.py` のみ。具体的には:

1. **特徴量** (`engineer_features` + `feature_cols`) — 最優先の探索軸
2. **賭け戦略** (`select_trifecta_bets`) — 次に重要
3. **モデル** (`LGBM_PARAMS`, 学習方法) — 三番目

大胆に変えてよい。一度に複数の変更をしてもよい。

## What you CANNOT do

- `prepare.py` を変更する
- パッケージを追加する
- CSVデータを変更する

## Execution

```bash
uv run python train.py > run.log 2>&1
grep "^roi:\|^hit_rate:\|^sharpe_ratio:\|^sortino:\|^mdd_pct:\|^max_losing_streak:\|^annual_profit:\|^ruin_prob:" run.log
```

1実験あたり約25秒。

## Judgment criteria

**keep** の条件（すべて満たす）:
- Sortino ratio が現在のベストを上回る
- ROI >= 100%（プラス収支）
- MDD が極端に悪化していない（目安: 現ベストの1.5倍以内）
- 破産確率(100単位) < 0.01

**keep（強い）**:
- Sortino改善かつMDD改善（両方良くなるのが最良）

**keep（弱い）**:
- Sortino改善だがMDD微悪化（トレードオフが許容範囲内）

**discard** の条件:
- Sortino低下、またはROI 100%割れ、またはMDDが大幅悪化

keep時は `git commit` して進む。discard時は `git checkout train.py` で戻す。

## Logging

results.tsvに毎回記録する（git管理外）:

```
experiment	roi	hit_rate	total_bets_races	total_bets_combos	annual_bets	avg_combos	sharpe	sortino	mdd_pct	max_lose_streak	annual_profit	status	description	monthly_roi_json
```

## Current state

**ベスト(制約付き): exp191 (Sortino 4.089, Sharpe 1.781, 年607R)**
- ROI 156.32%, 的中率 4.05%, 607R(年606.5), 利益+129,801円
- MDD 11.4%、最大連続赤字月 3
- MIN_TOP1_ODDS=3.3, 動的点数(VOL>=3.8→6点,堅め→2点), MIN_RACE_RUNNERS=9
- 全体確信度フィルタ: TOP3-4>=0.007, TOP1-4>=0.025, TOP1-2>=0.003, TOP2-3>=0.002
- 堅めレース追加フィルタ: TOP1-2>=0.005, TOP3-4>=0.008
- 特徴量: 単勝オッズ,人気,複勝シェア,複勝オッズ下限 + 前走系 + コース/馬属性(20個)
- モデル: LightGBM binary(3着以内), num_leaves=100, lr=0.10
- seed: 引数で指定可能 (`uv run python train.py [seed]`)

**参考(制約なし): exp85 (Sortino 9.072, 年89.7R)**
- 年600R制約を無視した場合の最高Sortino。確信度フィルタ4段でレースを極限まで絞る。

## Known findings

### 効いたこと
- **6段確信度フィルタ**: Sortino 3.703→4.089
  - 全体: TOP3-4>=0.007, TOP1-4>=0.025, TOP1-2>=0.003, TOP2-3>=0.002
  - 堅めレース追加: TOP1-2>=0.005, TOP3-4>=0.008
- 全レースTOP3ボックス6点（動的2/6→固定6）: 的中率倍増、Sortino大幅改善
- MIN_TOP1_ODDS引き上げ（3.3→3.8→4.0）: 荒れるレースに集中でROI・安定性改善
- 複勝市場の特徴量（複勝シェア、複勝オッズ下限）: 単勝オッズと補完的な情報
- 人気（順位）追加: オッズ値とは別のシグナル
- num_leaves=100: 表現力と正則化のバランス
- **4段確信度フィルタ**: モデルがTOP3を明確に区別できるレースのみ投資
  - TOP3-TOP4≥0.025: TOP3とTOP4の境界が明確
  - TOP1-TOP4≥0.07: 上位3頭が十分に分離
  - TOP2-TOP3≥0.01: 2着と3着の区別ができている
  - TOP1-TOP2≥0.01: 1着と2着の区別ができている
  - この「確信度フィルタ」が最大の改善源（Sortino 3.8→9.1）

### 効かなかったこと
- 特徴量追加は全般的にノイズ（現在の20特徴量が最適）:
  前走脚質、調教師年齢、市場乖離度、生後日数、前走上り3F、前走着差タイム、
  前走RPCI、前走場所、コース替え、斤量変化、距離変更率、同距離フラグ、
  オッズ評価変化、曜日、前走馬場状態、指定条件、レース番号R
- 特徴量削除も全般的に悪化（全20特徴量が不可欠）
- 特徴量一括追加: ノイズで悪化（1つずつ厳選追加が必要）
- multi-seed平均: 予測が平滑化されすぎて有害
- LambdaRank: binary classificationに劣る
- 正則化強化（lambda, min_child_samples変更）: 現設定が最適
- lr変更（0.08, 0.12）: lr=0.10がスイートスポット
- feature_fraction変更: 0.8がベスト
- 2モデルブレンド(top3+win): 単モデルに劣る
- target=2着以内: データ不足
- target=1着のみ: データ不足（過去知見）
- 血統タイプ・天気・コース区分: ノイズ
- 複勝オッズ幅/上限: 有害
- 複勝人気: 有害
- 前走複勝シェア・オッズ下限: ノイズ
- 前走人気: ノイズ
- 斤量体重比: 馬体重由来でリーク的
- 単複比: ノイズ
- オッズ変動率: NaN多で無効
- 多頭出し: データ欠損で無効
- MIN_RACE_RUNNERS変更(9/11/14): 10が最適（確信度フィルタとの相乗効果）
- VOLATILE_THRESHOLD変更(3.5, 3.6, 4.0, 4.5): 3.8が最適
- 前走距離差: ノイズ
- レース内オッズ標準偏差: ノイズ
- 前走上り3F順: ノイズ
- キャリア: ノイズ
- 馬体重増減: 大幅有害
- クラスコード: ノイズ
- 騎手コード/調教師コード: ノイズ
- 前走PCI3: ノイズ
- 前走平均1Fタイム: ノイズ
- 重量コード: ノイズ
- 所属コード: ノイズ
- 馬体重: ノイズ
- 前走確定着順（前走着順_numの代替）: 悪化
- Validation=2023年のみ: 学習データ減で悪化
- bagging_fraction=0.7/0.9: Sortino悪化、0.8がスイートスポット
- num_leaves 80/110/120: 100がスイートスポット
- 芝レースのみ: レース少なすぎ(年59)
- 枠番×芝ダ: ノイズ
- クラス変動（昇級/降級）: ノイズ
- 前走出走頭数: 壊滅的(max_lose 10)
- 騎手年齢: ノイズ
- 休み明け~戦目: ノイズ
- ゲート充足率: ノイズ
- Val 10%/25%: 20%が最適（10%→early stopping甘い、25%→学習データ不足）
- lambda_l1/l2変更(0.05, 0.2): 0.1が最適
- min_child_samples変更(15, 30): 20が最適
- feature_fraction変更(0.7, 0.9): 0.8が最適
- 堅めレース1点/3点: 2点が最適
- 3段階点数(6/4/2): 2段階(6/2)が最適
- 全レース6点統一: 2段階動的点数の方が良い
- 荒れレースで1着固定×TOP2-4: TOP3ボックスの方が圧倒的に良い
- 堅めレーススキップ: Sortino高いが年266Rで制約未達
- NUM_BOOST_ROUND=1000: early stoppingで同iteration、効果なし
- seed依存大: seed=42以外(123,7)ではSortino大幅低下
- multi-seed平均(5-seed): 予測平滑化で悪化（再確認済み）
- pred_std分散フィルタ: 既存gapフィルタに完全包含
- 重量種別(定量/ハンデ): ノイズ
- 曜日: ノイズ
- 前走4角通過順位: 壊滅的
- 2番人気オッズフィルタ(>=5.0): レース激減
- MIN_TOP1_ODDS=3.2: レース増えすぎでクオリティ低下
- bagging_freq変更(1,10): 5が最適
- 荒れレースTOP34フィルタ強化: 良いレースを落としてしまう
- 堅めレースTOP23厳格化: Sortino低下
- DART boosting: 遅くて劣る
- max_depth=8: Sortino悪化
- lr=0.05: 悪化
- num_leaves=90: 悪化
- 統合スコアフィルタ: 個別フィルタの方が良い
- MIN_TOP1_ODDS=4.2: レース年56で統計不足
- 予測スコア絶対値フィルタ: 効果なし（閾値が緩い/変化なし）
- log変換オッズ追加: LightGBMが自動学習するため冗長

## Exploration ideas（未試行）

### 特徴量
- 当日の全レース通じたオッズ変動トレンド（要実装が複雑）
- コーナー通過順位の活用（前走コーナー情報 — 位置取り傾向）
- 前走の着差タイムの正規化（距離やクラスで補正）

### 賭け戦略
- 月間の損失上限を設けるストップロス（バックテスト実装が複雑）
- ケリー基準ベースの動的賭け金（固定100円→推奨比率で変動）
- 確信度フィルタの閾値を時期によって動的に変更
- 荒れレースのTOP4含むフォーメーション（1着固定は失敗済み、別のフォーメーション案）

### モデル
- 時系列クロスバリデーション（現在は単純なtrain/val分割）
- seed安定化: 複数seedの「投票」ベース（平均ではなく）
- XGBoost/CatBoostとのアンサンブル

## NEVER STOP

ループを開始したら、手動で止められるまで永遠に回し続ける。
「続けていいですか？」と聞かない。アイデアが尽きたら、組み合わせを試す、
既存の知見を再検討する、より大胆な変更を試す。
