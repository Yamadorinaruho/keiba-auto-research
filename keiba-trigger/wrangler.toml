name = "keiba-trigger"
main = "src/index.js"
compatibility_date = "2026-05-01"
# cron専用。公開URL(workers.devサブドメイン)は不要。
workers_dev = false

# 3分毎に起動し、稼働時間帯(土日0-7 UTC=9-17 JST)の判定はWorker側で行う。
# (Cloudflareのcronは "*/3 0-7 * * 6,0" 形式を拒否するため単純式にしてコードで絞る)
[triggers]
crons = ["*/3 * * * *"]

[vars]
GITHUB_OWNER = "Yamadorinaruho"
GITHUB_REPO = "keiba-auto-research"
WORKFLOW_FILE = "summer-picks.yml"
GIT_REF = "main"
# 機密はsecretで(平文で置かない):
#   wrangler secret put GITHUB_TOKEN   # fine-grained PAT: Actions=Read and write
#   wrangler secret put TRIGGER_KEY    # 手動テスト用の任意キー(省略可)
